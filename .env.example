# Copy to .env for local configuration.
# Production should provide APP_MASTER_KEY from a secret manager.

# Optional 32-byte base64 key. If omitted, a local apps/web/data/master.key is generated.
# Generate with: node -e "console.log(require('node:crypto').randomBytes(32).toString('base64'))"
APP_MASTER_KEY=

# Secure by default (any value other than the literal string "false" counts as secure) — only
# set this to false for local HTTP dev with no TLS. Leaving it unset in a real deployment is safe;
# it does NOT fall back to insecure.
COOKIE_SECURE=false

# Inactivity timeout for authenticated sessions. Allowed range is 5-480 minutes.
SESSION_TIMEOUT_MINUTES=15

# Read by docker-compose.yml's postgres/migrate services, and by Prisma CLI commands (migrate
# deploy/dev, generate) wherever they run — these need ncb_medical_app specifically, the role that
# owns every table and therefore the only one with real DDL rights. Never used for the app's own
# live runtime queries in docker-compose (see APP_RUNTIME_DB_PASSWORD/DATABASE_URL below for that) —
# only local dev run directly with `npm run dev` (not through docker-compose's own `web` service)
# still uses this same role for its live connection too, for convenience; this is deliberately
# unset in docker-compose's own web service, so its distinction from the deployed default is
# something a developer running that command should notice, not something silently identical.
POSTGRES_PASSWORD=

# Read by Prisma CLI commands. Dotenv does not expand ${VAR} references, so paste the same
# password literally here as in POSTGRES_PASSWORD above.
# - Local dev against docker-compose's postgres service (host port 5433):
DATABASE_URL=postgresql://ncb_medical_app:<same-password-as-POSTGRES_PASSWORD>@127.0.0.1:5433/ncb_medical?schema=public
# - Inside docker-compose itself, the `migrate` service overrides this to point at the postgres
#   service by its container hostname instead; `web` overrides it to a completely different,
#   non-superuser role (see APP_RUNTIME_DB_PASSWORD below) rather than reusing this one.

# A genuinely separate, non-superuser Postgres role for the app's own live runtime queries — see
# DATABASE.md's "Security requirements" section. ncb_medical_app (above) is a Postgres SUPERUSER
# (it owns every table, created via migrations) — confirmed via external security review that using
# it for everyday runtime queries too means a compromise of that one live credential has full,
# unrestricted database access, GRANT/REVOKE bypass included. ncb_app_runtime has ordinary CRUD on
# every business table and explicitly none at all on audit_events (see migration
# 0026_app_runtime_role) — created by docker/postgres-init's init script on a fresh Postgres volume.
# Only used by docker-compose's own `web` service; local `npm run dev` still uses DATABASE_URL/
# ncb_medical_app above for simplicity.
APP_RUNTIME_DB_PASSWORD=

# A third, separate role used only for audit_events (see
# packages/database/src/client.ts's own createAuditPrismaClient() and DATABASE.md's "Security
# requirements" section) — created by docker/postgres-init's init script on a fresh Postgres volume,
# granted INSERT+SELECT on audit_events only by migration 0025_audit_writer_role. Generate a real
# password the same way you would for POSTGRES_PASSWORD; leave both blank and audit_events falls
# back to the main DATABASE_URL role instead (fine for quick local dev, not for a real deployment).
AUDIT_DB_PASSWORD=
AUDIT_DATABASE_URL=postgresql://ncb_audit_writer:<same-password-as-AUDIT_DB_PASSWORD>@127.0.0.1:5433/ncb_medical?schema=public
# - Inside docker-compose itself, the web service overrides this to point at
#   the postgres service by its container hostname instead.

# Read by docker-compose.yml's redis service (--requirepass) and by apps/web
# at runtime (REDIS_URL below) — same paste-the-same-value-twice reasoning as
# POSTGRES_PASSWORD/DATABASE_URL above.
REDIS_PASSWORD=

# - Local dev against docker-compose's redis service (host port 6380):
REDIS_URL=redis://:<same-password-as-REDIS_PASSWORD>@127.0.0.1:6380
# - Inside docker-compose itself, the web service overrides this to point at
#   the redis service by its container hostname instead.

# Better Auth's own signing secret (sessions, tokens) — separate from
# APP_MASTER_KEY, which encrypts application data, not auth tokens.
# Generate with: node -e "console.log(require('node:crypto').randomBytes(32).toString('base64'))"
BETTER_AUTH_SECRET=

# The app's own externally-reachable origin — Better Auth uses this for
# callback/redirect URLs instead of trusting the incoming request's Host
# header (which a proxy/spoofed request could otherwise influence). Falls
# back to http://localhost:3000 if unset, which is fine for local dev but
# MUST be set explicitly in any real deployment to your actual domain,
# e.g. https://portal.example.com.
BETTER_AUTH_URL=

# SMTP, notification templates, portal branding, and SLA definitions are
# configured through the Settings admin UI once logged in — not env vars.

# Malware scanning for uploaded case attachments is optional and deployment-specific — this app
# doesn't bundle a particular scanner, since which one (if any) is right depends on what the
# deploying organization already runs. Leave unset (the default) and attachments behave exactly as
# they did before this feature existed — no extra restriction, nothing falsely claimed "clean"
# either. To wire one in: implement the real call in apps/web/lib/virus-scan.ts's scanBuffer()
# (its own doc comment explains the contract), then set:
# VIRUS_SCAN_ENABLED=true
