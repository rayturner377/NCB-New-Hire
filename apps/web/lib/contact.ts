/** A person to reach by name and phone number — an emergency contact, a primary physician, etc. See components/form/contact-fields.tsx. */
export interface Contact {
  name: string;
  number: string;
}
